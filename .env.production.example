# Production Database (Supabase/Neon/Railway)
DATABASE_URL="postgresql://root:liveboard123@host:5432/liveboardDB?sslmode=require"

# Strong JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET="your-production-jwt-secret-32-chars-minimum"
JWT_EXPIRES_IN="7d"

# Server
PORT=4000
NODE_ENV="production"

# Frontend URL (Vercel/Netlify)
FRONTEND_URL="https://your-app.vercel.app"

# Redis (Railway/Upstash)
REDIS_URL="redis://user:password@host:port"
